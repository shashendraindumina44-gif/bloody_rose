module.exports = {
    cmd: ["hack2", "හැක්"],
    execution: async (sock, m, from, args) => {
        try {
            // Target කෙනෙක්ව අල්ලගමු
            let target = m.message.extendedTextMessage?.contextInfo?.mentionedJid?.[0] || 
                         m.message.extendedTextMessage?.contextInfo?.participant;

            if (!target) return sock.sendMessage(from, { text: "අනේ මැනික, හැක් කරන්න යාළුවෙක්ව Mention කරන්න. 😈" }, { quoted: m });

            const steps = [
                `🤫 සූදානම් වන්න... @${target.split("@")[0]} ව හැක් කරමින් පවතී...`,
                "🌐 IP ලිපිනය සොයාගත්තා: 192.168.1.254",
                "📩 WhatsApp පණිවිඩ කියවමින්: 15%...",
                "📁 ගැලරියට ඇතුළු වෙමින්: 48%...",
                "📸 අඩෝ මූ පට්ට කැත ෆොටෝනේ තියාගෙන ඉන්නේ! 🤮",
                "🔐 මුගේ Password එක '123456' ද? මුගේ මොළේ තරම ඉතින්... 😂",
                "✅ HACK COMPLETED! දැන් මුගේ ඔක්කොම විස්තර මගේ ළඟ!",
                "නිකන් විහිළුවක් කළේ මැනික.. මූ වගේ ගොනෙක්ව හැක් කරලා මට මොකටද? 🤭"
            ];

            // එකින් එක පියවර එවමු (Delay එකක් සහිතව)
            for (const step of steps) {
                await delay(2000); // තත්පර 2ක පරතරයක්
                await sock.sendMessage(from, { 
                    text: step,
                    mentions: [target]
                });
            }

            // Reaction එකක් දාමු
            await sock.sendMessage(from, { react: { text: '🤣', key: m.key } });

        } catch (e) {
            console.log("Hack Error:", e);
        }
    }
};

// Delay function එක (කෝඩ් එකේ පල්ලෙහාටම දාන්න)
function delay(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
}